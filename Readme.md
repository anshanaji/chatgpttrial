# Manuel Neuer Quiz App

This simple web app serves a 10-question quiz about Manuel Neuer. Scores are stored on a leaderboard based on the username entered before starting.

## Running

Install dependencies and start the Flask server:

```bash
pip install -r requirements.txt
python app.py
```

Then open `http://localhost:5000` in your browser.
